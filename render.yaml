services:
  - type: web
    name: gastrosena-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: FRONTEND_URL
        value: https://burbano52100.github.io
